apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapiapp.fullname" . }}-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: {{ include "fastapiapp.fullname" . }}-db
